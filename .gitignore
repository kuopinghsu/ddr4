# ── Verilator build artifacts ─────────────────────────────────────────────────
obj_dir/
*.o
*.d
*.a
*.mk

# ── Simulation / coverage outputs ─────────────────────────────────────────────
coverage.dat
annotated_cov/
annotated_cov_full/
logs/
*.vcd
*.fst
*.lxt2

# ── OS / editor noise ─────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

*.swp
*.swo
*~

# ── Node.js (wavedrom-cli dev deps, not committed) ────────────────────────────
node_modules/
package-lock.json
npm-debug.log*

# NOTE: docs/img/*.svg are intentionally NOT ignored.
# They are auto-generated by CI (.github/workflows/wavedrom.yml) and committed
# so GitHub can render them inline in docs/ddr4_parameters.md.
